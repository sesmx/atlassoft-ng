import { ZeroprefixPipe } from './zeroprefix.pipe';

describe('ZeroprefixPipe', () => {
  it('create an instance', () => {
    const pipe = new ZeroprefixPipe();
    expect(pipe).toBeTruthy();
  });
});
